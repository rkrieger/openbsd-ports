To completely deinstall the mod_sflow package you
need to perform these steps as root:
       
	edit /etc/apache2/httpd2.conf
	remove the line: LoadModule sflow_module

Do not do this if you plan on re-installing the mod_sflow
package at some future time.
